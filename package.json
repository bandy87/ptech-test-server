{
  "name": "socketio-server",
  "version": "1.0.0",
  "description": "Test Chat server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "*",
    "shortid": "^2.2.8",
    "socket.io": "*"
  }
}
